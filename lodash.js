var lodash = require('lodash');

var collection = [74, 17, 69, 2004, 7, 2, 22, 9, 25];

// make use of lodash shuffle function
// console.log(lodash.shuffle(collection));

// make use of lodash mean function
// console.log(lodash.mean(collection));

// make use of lodash max function
//console.log(lodash.max(collection));

// make use of lodash random function
console.log(lodash.random(0, 89));